# Google OAuth Configuration
# Get credentials from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# 
# Setup steps:
# 1. Create a new project (or use existing)
# 2. Configure OAuth consent screen (External, add email/profile scopes)
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized redirect URI: http://localhost:5000/signin-google
# 5. Copy Client ID and Client Secret below

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys
#
# OpenRouter provides access to multiple LLM models through a single API
# Used for the AI Dungeon Master assistant in Project Riddle

OPENROUTER_API_KEY=your-openrouter-api-key
